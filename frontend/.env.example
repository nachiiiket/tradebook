# Optional — only needed for local dev if not using next.config rewrites default
# On Vercel, API is same-origin (/api) — leave this unset
# NEXT_PUBLIC_API_URL=

# Local Django URL used by Next.js rewrites (npm run dev)
DJANGO_API_URL=http://localhost:8000
